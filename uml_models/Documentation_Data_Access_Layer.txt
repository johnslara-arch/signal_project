This is an Adapter design pattern which enables input to be translated to a standardized form for the DataStorage system to handle. It is separated from the main code through the DataSourceAdapter, which calls the DataParser to actually standardise the input with the getAdaptedInput() method. The DataParser chooses a concrete DataListener to standardise the input from based on the external data source.