The data storage system calls the PatientIdentifier to match its records to Hospital patients. The patient identifier uses the IdentityManager to check whether there is a match to an existing patient or not through the matchPatient() method. This means the IdentityManager handles exceptions and edge cases.