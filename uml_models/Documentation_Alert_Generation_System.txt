This system uses a Strategy design pattern to allow for multiple AlertEvaluator strategies to determine whether an alert should be created (e.g. heart rate, blood pressure...). The AlertGenerator sets the chosen strategy through the interface and then creates the relevant alert through the evaluateData method if the conditions are met. Finally, it calls the AlertManager in the same method, which notifies all necessary stakeholders of any alerts created.